# This directory holds JSON data files written by the bot after each scan cycle.
# These files allow Streamlit Cloud to display live data without a separate database.
#
# Files written here by polyedge/exporter.py:
#   signals.json   — last 100 signals
#   trades.json    — last 200 trades
#   portfolio.json — current portfolio snapshot
#
# DO NOT delete this directory. The JSON files are auto-updated by GitHub Actions.
